import { CiSearch } from "react-icons/ci";

const SearchProduct = () => {
  return (
    <span className="search-icon">
      <CiSearch className="mr-10 text-primary" size={30} />
    </span>
  );
};

export default SearchProduct;
